/**
 * GitHub Pages landing: set this to your deployed backend URL (no trailing slash).
 * Example: https://real-agent-api.onrender.com
 * After deploy, commit and push so "Login" / "Get Started" point to the live app.
 */
window.REAL_AGENT_API = "";
